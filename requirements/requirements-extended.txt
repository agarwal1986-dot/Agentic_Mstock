# --- Core dependencies ---
-r requirements-core.txt

# --- Low-level utilities & compatibility ---
# Attribute definitions and lightweight data classes
attrs==24.2.0
# Finite state machine library used by Twisted
Automat==24.8.1
# Constants for Twisted event system
constantly==23.10.4
# Versioning utilities for Twisted
incremental==24.7.2
# Python 2/3 compatibility helpers
six==1.16.0
# Typing extensions for forward compatibility
typing_extensions==4.12.2
# Colored terminal text output
colorama==0.4.6

# --- Networking & HTTP stack ---
# Root certificates for SSL verification
certifi==2024.8.30
# Internationalized domain names support
idna==3.10
# Character encoding detection
charset-normalizer==3.4.0
# HTTP client base library
urllib3==2.2.3
# URL parsing and manipulation
hyperlink==21.0.0
# Mocking HTTP responses for testing
responses==0.25.7

# --- Security & cryptography ---
# Foreign Function Interface for C extensions
cffi==1.17.1
# C parser used by cffi
pycparser==2.22
# Cryptographic recipes and primitives
cryptography==43.0.3
# OpenSSL wrapper for Python
pyOpenSSL==25.0.0
# ASN.1 encoding/decoding
pyasn1==0.6.1
# ASN.1 modules for security protocols
pyasn1_modules==0.4.1
# Service identity verification for TLS
service-identity==24.2.0

# --- Twisted ecosystem (networking framework) ---
# Interface definitions for Twisted
zope.interface==7.1.0
# Event-driven networking engine
Twisted==24.7.0
# Asynchronous I/O helpers
txaio==23.1.1
# WebSocket and WAMP networking library
autobahn==24.4.2

# --- Packaging & build tools ---
# Python package management and distribution
setuptools==76.1.0
# Standardized package metadata
packaging==24.2
# Plugin management for pytest and other tools
pluggy==1.5.0
# Config file parser for pytest
iniconfig==2.1.0
# TOML parser for Python
tomli==2.0.2

# --- LangChain ecosystem (Agent & Graph frameworks) ---
# High-level orchestration for LLM applications
langchain==1.1.0
# Core abstractions for chains, prompts, and models
langchain-core==1.1.3
# Utilities for splitting text into chunks
langchain-text-splitters==1.0.0
# Experiment tracking and debugging
langsmith==0.4.59
# Legacy support for older LangChain APIs
langchain-classic==1.0.0
# Community-contributed integrations
langchain-community==0.4.1
# Graph-based orchestration for agent workflows
langgraph==1.0.4
# Prebuilt graph templates for common agent patterns
langgraph-prebuilt==1.0.5

# --- Data Processing & ML ---
# Data manipulation and analysis
pandas==2.3.3
# Machine learning algorithms and preprocessing
scikit-learn==1.8.0
# Scientific computing (linear algebra, stats, optimization)
scipy==1.16.3
# Efficient model persistence and parallel computation
joblib==1.5.2
# Numerical computing library
numpy==1.26.4
# Protocol buffers for ML frameworks
protobuf==4.25.3

# --- Deep Learning & NLP ---
# PyTorch deep learning framework
torch==2.9.1
# Hugging Face library for pretrained NLP models
transformers==4.46.3
# Fast tokenization library for NLP
tokenizers==0.20.3
# Safe and efficient tensor serialization format
safetensors==0.7.0
# TensorFlow deep learning framework
tensorflow==2.15.1
# Intel-optimized TensorFlow build for Windows
tensorflow-intel==2.15.1
# Sentence embeddings for semantic search
sentence-transformers==3.0.1

# --- Optional: Vector Stores (Hybrid approach) ---
# FAISS: High-performance vector similarity search (CPU build, Windows-friendly)
faiss-cpu==1.9.0
# ChromaDB: Lightweight vector database for embeddings, easy integration with LangChain
chromadb==0.5.23
# hnswlib==0.8.0   # requires MSVC build tools; manual compilation on Windows (keep commented unless needed)

# --- Data Management ---
# ORM for structured database access
SQLAlchemy==2.0.45
# YAML parsing for config files
PyYAML==6.0.2

# --- API & Web Server ---
# FastAPI framework for building APIs
fastapi==0.115.0
# ASGI server for FastAPI
uvicorn==0.32.0
# Data validation and settings management
pydantic==2.9.2

# --- Trading API SDK (mStock) ---
# Official mStock Trading API SDK (Type A)
mStock-TradingApi-A

# --- Trading API SDK (mStock) ---
# Official mStock Trading API SDK (Type B)
mStock-TradingApi-B==1.0.5


# --- CLI & Development ---
# CLI app builder (based on type hints)
typer==0.20.0
# Command-line interface toolkit
click==8.3.1
# Rich text and formatting in terminal
rich==13.9.4
# Human-friendly colored logging output
coloredlogs==15.0.1

# --- Monitoring & Observability ---
# Observability API for tracing/metrics/logs
opentelemetry-api==1.27.0
# SDK implementation for OpenTelemetry
opentelemetry-sdk==1.27.0
# Export telemetry data via gRPC
opentelemetry-exporter-otlp-proto-grpc==1.27.0
# Product analytics and event tracking
posthog==7.0.1

# --- Environment & Config ---
# Environment variable loader
python-dotenv==1.2.1

# --- Testing & QA ---
# Testing framework
pytest==8.3.5
# Async testing support
pytest-anyio==0.0.0